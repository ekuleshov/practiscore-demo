
The classifier definitions and images need to be moved out of the app
and instead should be hosted on the website.

Can either download a hosted *.psc match files (using url from the match template)
or go trough pin# import with special pin#.

